import "dart:io";

// Add each name to a list
void main(){ 
  print("Enter Friend Name");
  List<String?> friendName = [];
  String? name = stdin.readLineSync();
    friendName.add(name);
    friendName.add(name);
    friendName.add(name);
}