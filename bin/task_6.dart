// Bonus Task: Try printing each friends name using friends[0], friends[1], etc.
// Try printing with .toUpperCase()
void main(){
 List<String?> friendName = ["Abiola", "Abayomi", "Oluwayomi"];

print(friendName[0]?.toUpperCase());
print(friendName[1]?.toUpperCase());
print(friendName[2]?.toUpperCase());
}